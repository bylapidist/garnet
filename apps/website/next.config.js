/** @type {import('next').NextConfig} */
const nextConfig = {
    /* config options here */
    output: 'export',
    distDir: 'dist',
    reactStrictMode: true,
    assetPrefix: './',
};

module.exports = nextConfig;
